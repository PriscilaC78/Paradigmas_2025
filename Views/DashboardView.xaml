<UserControl x:Class="proyecto_paradigmas_2025.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Panel Principal" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <WrapPanel>
                <Border Background="White" Width="200" Height="100" Margin="10" CornerRadius="5">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Equipos en Taller" Foreground="Gray"/>
                        <TextBlock Text="{Binding CantidadEnTaller}" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Background="White" Width="200" Height="100" Margin="10" CornerRadius="5">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Listos para Entregar" Foreground="Gray"/>
                        <TextBlock Text="{Binding CantidadListos}" Foreground="Green" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>